<%= form_with(model: payer_plan) do |form| %>
  <% if payer_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payer_plan.errors.count, "error") %> prohibited this payer_plan from being saved:</h2>

      <ul>
        <% payer_plan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :payer_id %>
    <%= form.number_field :payer_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
