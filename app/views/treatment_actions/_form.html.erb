<%= form_with(model: treatment_action) do |form| %>
  <% if treatment_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(treatment_action.errors.count, "error") %> prohibited this treatment_action from being saved:</h2>

      <ul>
        <% treatment_action.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :action %>
    <%= form.text_field :action %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
